<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
  <% end %>
</div>

<div>
  <html>
<H1>
  <h1>@student.first_name + " " + s@tudent.last_name %></div></h1>
</html>
